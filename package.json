{
  "name": "browser-dialogs-in-react",
  "version": "1.0.0",
  "description":
    "Minimal implementation leveraging `window.alert`, `window.confirm` and `window.prompt` that would allow you to replace them with React components without changing the API",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "flow-bin": "0.76.0",
    "lodash": "4.17.10",
    "react": "16.3.2",
    "react-dom": "16.3.2",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
